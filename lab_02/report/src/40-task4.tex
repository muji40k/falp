\leftsection{Задание}
Написать функцию longer-then от двух списков-аргументов, которая возвращает Т,
если первый аргумент имеет большую длину.

\begin{lstlisting}[language=Lisp]
(defun longer-then (a b) (> (length a) (length b)))
\end{lstlisting}

